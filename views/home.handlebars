<nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4 py-3 shadow-sm">
  <div class="container-fluid d-flex justify-content-between align-items-center">
    <h1 class="navbar-brand mb-0">📇 Lista de Contatos</h1>
    
    <form class="d-flex align-items-center" action="/search" method="get">
      <input 
        class="form-control me-2" 
        type="search" 
        name="q" 
        placeholder="Buscar por nome, email ou telefone..." 
        aria-label="Buscar contato"
      >
      <button class="btn btn-success" type="submit">
        Buscar
      </button>
    </form>

    <a href="/create" title="Adicionar contato">
      <img src="/imgs/userAdd.png" alt="Adicionar contato" class="img-fluid icon">
    </a>
  </div>
</nav>

<div class="container mt-4">
  <div class="table-responsive rounded shadow-sm border" style="max-height: 500px; overflow-y: auto;">
    <table class="table table-striped table-hover align-middle mb-0">
      <thead class="table-dark sticky-top">
        <tr>
          <th scope="col">Nome</th>
          <th scope="col">Email</th>
          <th scope="col">Telefone</th>
          <th scope="col" class="text-center">Ações</th>
        </tr>
      </thead>
      <tbody>
        {{#each contatos}}
          <tr>
            <td>{{this.nome}}</td>
            <td>{{this.email}}</td>
            <td>{{this.telefone}}</td>
            <td class="text-center">
              <a href="/edit/{{this.id}}" class="me-2" title="Editar">
                <img src="/imgs/userUpdate.png" alt="Editar contato" class="img-fluid icon" style="width: 24px;">
              </a>
              <a href="/delete/{{this.id}}" title="Excluir" onclick="return confirm('Tem certeza que deseja excluir este contato?')">
                <img src="/imgs/userDelete.png" alt="Excluir contato" class="img-fluid icon" style="width: 24px;">
              </a>
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>
